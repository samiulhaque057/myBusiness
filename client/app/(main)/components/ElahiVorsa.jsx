import { siliguri } from "@/app/layout";

export default function ElahiVorsa({ style }) {
  return (
    <h1
      className={`text-center ${style} pt-3 font-bangla text-3xl sm:text-4xl font-semibold ${siliguri.variable}`}
    >
      এলাহী ভরসা
    </h1>
  );
}
